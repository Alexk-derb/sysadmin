# Журнал рефакторинга — память между сессиями

> Агент пишет сюда после КАЖДОЙ задачи. Если чат прервётся — новый агент читает этот файл и
> продолжает с места. Это источник правды о прогрессе. Не переписывай задним числом — только
> дописывай.

## Статус

- **Текущий этап:** 3 — «Полировка»
- **Текущая задача:** Этап 4, задача 4.1 (онбординг под реального новичка)
- **Последнее обновление:** 13.06.2026, Этап 3 ЗАКРЫТ проверщиком (PASS 7/7)
- **Версия на старте:** 1.10.0

## Чеклист этапов (отмечать по мере PASS проверщика)

- [x] Этап 0 — Сначала не навреди (→ 1.11.0) — PASS проверщика 13.06.2026, 10/10 пунктов
- [x] Этап 1 — Генеральная уборка (→ 1.12.0) — PASS проверщика 13.06.2026, 9/9 пунктов
- [x] Этап 2 — Архитектура 2.0 (→ 2.0.0) — PASS проверщика 13.06.2026, 13/13, регрессий нет
- [x] Этап 3 — Полировка (→ 2.1.0) — PASS проверщика 13.06.2026, 7/7 пунктов
- [ ] Этап 4 — Продукт (→ 2.2.0)
- [ ] Этап 5 — Backlog (без релиза)

---

## Хронология (дописывать сверху вниз)

### [2026-06-13] Этап 0, задача 0.1 — обезврежен migrate-server-to-server (D1)
- Что сделано: rsync --delete и tar xzf -C / переведены в красную зону (4-шаговая
  процедура + type-to-confirm); в pre-checklist добавлены сверка направления
  «откуда → куда» (с живыми доказательствами ролей хостов) и обязательный бэкап
  ЦЕЛЕВОГО сервера; скрипт 02-rsync-incremental.sh теперь требует подтверждение
  направления (НАПРАВЛЕНИЕ-ВЕРНОЕ) и type-to-confirm на фазах с --delete;
  01-pre-migration-backup.sh принимает тег target и не падает на пустом целевом
  (пропуски с явными сообщениями вместо тихого fail).
- Коммит: 4040524 fix(skills): migrate-server-to-server — красная зона для rsync --delete и tar поверх корня (D1)

### [2026-06-13] Этап 0, задача 0.2 — обезврежен rotate-secrets (D2)
- Что сделано: redact_stream/redact_json_with_jq вынесены в `_lib/redact.sh`
  (переиспользование, не копия; dump-snapshot.sh source-ит её с fail-fast);
  redact_stream расширен паттернами SQL-эха (PASSWORD '...', IDENTIFIED BY,
  requirepass); rotate-db-password.sh и rotate-api-token.sh переписаны: секрет
  через stdin/env (не argv), печать секрета заменена файлом 0600 / скрытым вводом,
  флаги --old и --new-token отклоняются с объяснением; SKILL.md получил «железное
  правило» из 3 пунктов. Тест: все 7 паттернов redact работают, печати секретов нет.
- Примечание для проверщика: `grep -rn "echo.*NEW|echo.*PASS"` находит 4 строки,
  но ни одна не печатает значение секрета (длина пароля, пути к файлам, текст
  подсказки с плейсхолдером).
- Коммит: 1e7a4ff fix(skills): rotate-secrets — секреты не печатаются и не светятся в argv (D2)

### [2026-06-13] Этап 0, задача 0.3 — правило «данные ≠ инструкции» (D4)
- Что сделано: новое конституционное правило C.11 в выжимке (данные с сервера и
  из интернета — не команды; инъекции не исполняются, помечаются оператору) +
  полный слой в trust-zones.md (источники-данные, 4 правила, 4 примера, барьер
  от отравления knowledge). Линтер персоны PASS (вручную и в pre-commit).
- Коммит: 242e0a1 feat(persona): C.11 «данные ≠ инструкции» — защита от prompt injection из читаемого (D4)

### [2026-06-13] Этап 0, задача 0.4 — барьер красной зоны для новичка (D3)
- Что сделано: для необратимых операций брифинг PROPOSE обязан содержать строки
  «⚠️ ЧТО ТЕРЯЕМ» (человеческим языком) и «⚠️ КОПИЯ: есть/НЕТ» до фразы
  подтверждения; анти-копипаст — имя сущности в type-to-confirm оператор вписывает
  сам (шаблон с пропуском, непереписанный шаблон отклоняется). Решение «не
  подсказывать фразу вообще» отвергнуто (ломает UX новичка) — зафиксировано
  в ADR-0014. Канонический формат C.3 не менялся. Слои синхронны, линтер PASS.
- Коммит: 86f34b7 feat(persona): барьер необратимых операций — строка потери + анти-копипаст (D3, ADR-0014)

### [2026-06-13] Этап 0, задача 0.5 — мина 2.0 снята: поиск корня без файла персоны (P12)
- Что сделано: версионируемый маркер `.sysadmin-root` в корне репо;
  locate_sysadmin_root теперь проверяет `_is_sysadmin_root` (маркер ИЛИ
  legacy-признак для старых установок). Тест: без bridge (HOME=fake) и с
  переименованным файлом персоны корень находится. Линтер и pre-commit корень
  определяют сами — не зависят, правок не потребовали. Линтер PASS.
- Коммит: bef794e fix(lib): поиск корня мозга отвязан от файла персоны — маркер .sysadmin-root (P12)

### [2026-06-13] Этап 0, задача 0.6 — _live/ выведен из git (P13)
- Что сделано: выбран вариант 1 из плана (gitignore, проще для существующих
  установок): `_live/*.md` в .gitignore (кроме `*.example.md`), 5 файлов сняты
  с трекинга (`git rm --cached` — на диске остались, знания Василия не тронуты),
  добавлены шаблоны frontline.example.md + timeline.example.md и .gitkeep.
  Тест: правка frontline-ru.md не грязнит дерево (check-ignore OK).
  refresh-vpn-knowledge пути не меняет — пишет в те же файлы, git не трогает.
- Коммит: d995585 fix(knowledge): слой _live выведен из git — шаблоны вместо живых файлов (P13)

### [2026-06-13] Этап 0, задача 0.7 — протечки апгрейда (P17/P19/P20)
- Что сделано: .gitignore ловит *.bak/*.bak.* и конфликтные копии (рус+англ);
  cold-start.md — git add . заменён адресным добавлением, добавлено
  предупреждение про .git в облаке при двух машинах; refresh-vpn-knowledge —
  AUTO_REFRESH_DATES default=false, «нет» не освежает дату, правила честной
  даты в шаге 5. Проверки: check-ignore OK ×3, голого git add . в персоне нет,
  линтер PASS.
- Коммит: cdf9248 fix(upgrade): закрыты протечки апгрейда — gitignore, git add ., авто-дата, .git в облаке (P17/P19/P20)

### [2026-06-13] Этап 0, задача 0.8 — UPGRADE.md и честная документация обновления (P18)
- Что сделано: UPGRADE.md (механизм: fetch + checkout --detach на тег, проверка
  раз в сутки; таблица переходов 1.10.x / 1.4-1.9 / ≤1.3 → 1.11.0; поле version
  «1.0» уже существовало в обоих схемах с ADR-0013 — требование «добавить поле
  версии формата» оказалось выполненным ранее, зафиксировано объяснение
  legacy-детекта по имени). README/INSTALL: «раз в 14 дней»→«раз в сутки»,
  «git pull»→реальный механизм. Ссылки на UPGRADE.md: CLAUDE.md (карта) + README
  (дерево + блок обновлений) + INSTALL (2 места).
- Коммит: 29e70b6 docs(upgrade): UPGRADE.md — механизм обновления и таблица переходов версий (P18)

### [2026-06-13] Этап 0, задача 0.9 — бамп 1.11.0 + CHANGELOG
- Что сделано: VERSION 1.10.0→1.11.0; CHANGELOG: раздел 1.11.0 с честным
  описанием (Безопасность D1/D2/D4/D3 + редакция-библиотека, Добавлено
  .sysadmin-root/UPGRADE.md/шаблоны _live, Изменено P13/P17/P19/P20,
  Совместимость).
- Коммит: 29f81b6 chore(release): 1.11.0 — этап 0 «Сначала не навреди»

### [2026-06-13] Этап 1, задача 1.1 — уборка корня (P4, P26)
- Решение оператора: вариант «архив docs/archive/» (а не вынос из репо совсем).
- Что сделано: 6 процессных отчётов (AUDIT-2026-05-16, AUDIT-METHODOLOGY,
  CHECK-PLAN, REVIEW-2026-05-16-DONE, SESSION-VPN-2026-05-15,
  SESSION-VPN-LIVE-KNOWLEDGE-2026-05-17) + папка `.planning/` (4 рисёрча +
  release-notes) перенесены `git mv` в `docs/archive/` (история сохранена).
  Ссылки на них в ADR-0001…0006 переведены на новый путь (висячих ссылок нет —
  проверено grep). Легаси-шаблоны `sysadmin-config.example.json` +
  `sysadmin-config.schema.json` удалены (`git rm`, заменены agent/infra-config
  по ADR-0013). Создан `docs/archive/README.md` + ссылка из карты репо в CLAUDE.md
  (правило «документ без ссылки — неполная задача»). `.DS_Store` уже был в
  .gitignore и не в git. Корень: 14 tracked-файлов, все легитимные. Линтер PASS.
- Примечание: легаси-ссылки `sysadmin-config.*` в живом коде (find-config.sh:102,
  cold-start.md:71) НЕ трогал в этой задаче — они в зоне 1.2 (чистка текстов скиллов).
  Исторические упоминания в ADR-0005 (Этап 8) оставлены как легитимная история.
- Коммит: 5529a4c chore(repo): уборка корня — процессные артефакты в docs/archive, удалены легаси-шаблоны конфига (1.1)

### [2026-06-13] Этап 1, задача 1.2 ч.1 — честные числа и имена (P5, центральные доки)
- Что сделано: README версия 1.0.1→1.11.0 (×2), cap ядра 400→460 (ADR-0002→ADR-0012),
  «около 460 строк». INSTALL/CONTRIBUTING «18 скиллов»→«20» (реальный счёт = 20,
  проверено find). Персона sysadmin.md: шапка ядра теперь цитирует cap 460/ADR-0012
  синхронно с врезкой в конце (была 400/ADR-0002). first-run.md: имена конфигов
  переведены на agent-config.json/infra-config.json (legacy как fallback). cold-start.md:
  удалённая legacy-схема — валидация legacy пропускается, предлагается миграция.
  find-config.sh: шапка + сообщение «не найден» + комментарий → agent-config.schema.json
  (legacy-перебор путей НЕ тронут — он корректен, ищет оба имени). 3x-ui-api.md:
  panel_api_token из infra-config.json. sysadmin-meet: убрано обещание несуществующего
  /create-skill (×2) — fresh-clone его не содержит. Линтер PASS.
- Найдено по ходу (в 2.4): ядро персоны фактически 465 строк при cap 460 (ADR-0012) —
  перебор на 5 строк. Не правлю surgically сейчас: механизм бюджета (КБ + pre-commit)
  строится в задаче 2.4, там же ребаланс/решение по cap. Доки цитируют канонический
  cap 460 (это честно — это cap, а перебор — известный долг).
- Коммит: c8e39c4 fix(docs): честные числа и имена — версия 1.11.0, 20 скиллов, cap ядра 460, имена конфигов (1.2 ч.1)

### [2026-06-13] Этап 1, задача 1.2 ч.2 — config-name по скиллам (P5, в работе через workflow)
- Что делается: ~12 папок скиллов с устаревшими `sysadmin-config.json` → правильный
  файл по таблице полей ADR-0013 (secrets.manager/language/operator → МОЗГ agent-config.json;
  servers/vpn/backups/monitoring/notifications → КАРТА infra-config.json). Legacy/миграционные
  упоминания (sysadmin-init, sysadmin-meet/finale, find-config legacy-пути, validate-config,
  self-test) НЕ трогаются — там старое имя легитимно. Запущен workflow (1 агент на папку,
  строгий рубрик, аудируемый отчёт). Workflow завершён: 12 агентов,
  remaining_stale_count=0 у всех, маппинг корректный (критичное secrets.manager/
  language/operator → МОЗГ соблюдено везде). Независимая верификация grep: в 12
  папках 0 литералов sysadmin-config; по всему репо остались только легитимные
  (legacy-fallback с явным «legacy», миграция в sysadmin-init/sysadmin-meet/finale,
  ADR-история, схемы-«переименован из»). Поймано вне 12 папок: scripts/lib-api/
  3xui.sh — panel_api_token → infra-config.json (починено в этом же коммите).
- Коммит: 13bf9c3 fix(skills): config-name по ADR-0013 + опечатки в текстах скиллов (1.2 ч.2, 1.3)

### [2026-06-13] Этап 1, задача 1.3 — опечатки и артефакты (P6)
- Что сделано: «menager»→«менеджер[а]» (×6, с учётом падежа: genitive «менеджера
  паролей», «из менеджера»; accusative «в менеджер оператора») в generate-client-config,
  setup-vpn-panel (+panel-hardening), configure-vpn-routing. «simulationperia»→«простоя»
  (configure-vpn-routing §ЧТО ПРОИЗОЙДЁТ: 1-2 сек простоя при рестарте Xray).
  Доп.прогон mixed-script детектором по всем скиллам нашёл «sortировка»→«сортировка»
  (cleanup-existing-server/typical-grabli.md). Закоммичено вместе с 1.2 ч.2 (файлы
  пересекаются — те же SKILL.md).
- Коммит: 13bf9c3 (см. выше, общий с 1.2 ч.2)

### [2026-06-13] Этап 1, задача 1.4 — прогон тестов + починка eval-харнеса (P7)
- ГЛАВНОЕ найдено-и-починено: eval-харнес был НЕВАЛИДЕН. Headless
  `claude -p --disable-slash-commands` НЕ подгружает локальные скиллы в контекст
  диспетчера → модель выдумывала несуществующие имена («security-audit» вместо
  «audit-security», «install-3xui-panel» вместо «setup-vpn-panel»). Весь прогон
  давал ложный массовый FAIL. Фикс: invoke-claude.sh теперь передаёт в системный
  промпт ЗАКРЫТЫЙ список реальных имён скиллов (коммит e7cb3e5). Прогон вскрыл
  второй баг — пробел в пути репо («Claude Code») ломал find|xargs|basename,
  список мусорился токеном «Claude»; пофикшено robust-перебором globом (коммит
  04e8c0e). После: список = ровно 20 чистых имён, 4/4 диагностики точны.
- Честная зелёная точка (первая ВАЛИДНАЯ): представительный прогон
  `run-evals.sh --all --sample 2` (19 скиллов с evals × 6 фраз = 114 вызовов).
  **104 PASS / 10 FAIL (91%)**, стоимость $0 (подписка Claude Code, не per-call —
  оценка «$111» из --estimate к подписке неприменима). Отчёт:
  `.eval-results/20260613T025915Z.md`.
- 10 провалов классифицированы — НИ ОДИН не регресс этапа 1 (я не трогал триггеры/
  description скиллов, только тела и имена конфигов): (а) терсные/непрямые positive
  → none (модель осторожничает): «первый запуск»/sysadmin-init, «помоги
  разобраться с агентом»/sysadmin-meet, «накати обновление»/deploy-service, «бот
  не может в Claude API»/setup-server-proxy; (б) пересечение VPN-скиллов: «добавь
  клиента VPN» → generate-client-config вместо configure-vpn-routing; (в) negative-
  пробы границ: «восстанови данные из бэкапа»→setup-backups (мотивирует
  restore-скилл = задача 3.1), fix-vs-audit, файловый бэкап, app-метрики Grafana.
  Это вопросы качества маршрутизации/description (сам отчёт пишет «чинить
  description») → бэклог полировки скиллов (этап 3/4); триггеры/eval-данные
  молча НЕ меняю (это смена поведения продукта, требует ревью оператора).
- Коммиты: e7cb3e5 + 04e8c0e (см. выше).

### [2026-06-13] Этап 1, задача 1.5 — бамп 1.12.0 + CHANGELOG
- Что сделано: VERSION 1.11.0→1.12.0; раздел [1.12.0] в CHANGELOG с честным
  описанием этапа 1 (Удалено/перемещено: docs/archive + легаси-шаблоны;
  Исправлено: честность доков P5, config-name ADR-0013 в 12 скиллах, опечатки P6,
  eval-харнес P7 + baseline 104/114; Совместимость: legacy читается).
- Коммит: bfdc21f chore(release): 1.12.0 — этап 1 «Генеральная уборка»

### [2026-06-13] Этап 2 — архитектурная развилка решена оператором (вход 2.0)
- Исследование claude-code-guide подтвердило реальность механизмов: `"agent": "sysadmin"`
  в settings.json РЕАЛЬНО делает агента главным тредом при открытии папки; CLAUDE.md
  грузится как user-message (целевой лимит ~200 строк, слабее system-prompt); главный
  агент имеет ВСЕ инструменты (включая Skill) по умолчанию — у субагента sysadmin.md
  сейчас в `tools:` НЕТ Skill (поэтому как субагент он скиллы вызвать не мог).
- Вскрыто противоречие плана: 2.1 «персона → CLAUDE.md + удалить agent-файл» против
  2.3 «settings.json agent: sysadmin» (нужен agent-файл). Вынес оператору 3 варианта.
- **Решение оператора: вариант А — персона → CLAUDE.md (буква плана 2.1).**
- Как исполняю А (резолвлю противоречие в пользу CLAUDE.md как носителя):
  • CLAUDE.md = персона сисадмина (на диете по 2.1: рефлекс = триггер+правило+указатель,
    детали в references/) + универсальные правила безопасности (не выдумывать, секреты,
    коммиты). Это «кто ты по умолчанию, открыв папку».
  • Текущие DEV-правила CLAUDE.md (синхрон слоёв, изменил скиллы→доки, документ без
    ссылки, ADR-дисциплина, карта репо) → переезжают в скилл `/dev` (задача 2.2).
  • Главный агент (через CLAUDE.md) автоматически имеет Skill → проблема «скиллы не
    работают» решается даром; settings.json `agent` как механизм входа НЕ нужен (вход —
    через CLAUDE.md). 2.3 адаптирую: settings.json минимальный, без обязательного `agent`.
  • agent-файл sysadmin.md → тонкая заглушка-указатель на CLAUDE.md (для @sysadmin
    из чужих папок; не удаляю — discovery по name).
  • Линтер check-persona-sync.sh: PERSONA переводится с sysadmin.md на CLAUDE.md.
    cold-start §1 (поиск персоны), pre-commit — обновляются.
- Зафиксировано в ADR-0015.

### [2026-06-13] Этап 3, задача 3.1 — восстановление из бэкапа (не ждёт инцидента, P3)
- Создан скилл `/restore-from-backup`: restic/rclone-стек (как `/setup-backups`), passphrase
  из менеджера паролей. Безопасная схема — сначала restore во ВРЕМЕННОЕ место + сверка
  (row counts), промоушен в прод = красная зона (4-шаговая процедура, type-to-confirm,
  строки «ЧТО ТЕРЯЕМ»/«КОПИЯ»). Шаг R — плановый restore-тест: «бэкап без проверенного
  restore = не бэкап»; >30 дней без теста → считаю непроверенным.
- Рефлекс персоны: компактно в CLAUDE.md §4.1 (бэкап без restore = не бэкап, >30 дн →
  непроверен) + полно в ritual-life.md (ежемесячный обход §4, ссылка на /restore-from-backup).
  Бюджет ядра: 38495/42000 (остаток 3505). Линтер PASS.
- Доки (правило «изменил скиллы → обнови доки»): счёт 20→21 операционных скиллов во всех
  местах (README, INSTALL, CONTRIBUTING, sysadmin-meet — с учётом падежей/согласования),
  restore добавлен в README-таблицу + дерево + ELI5-описание sysadmin-meet/03-skills.md
  (Группа 2). Скилл-директорий: 22 (21 операционный + dev).
- Коммит: baf8409 feat(skills): /restore-from-backup + рефлекс «бэкап без restore = не бэкап»

### [2026-06-13] Этап 3, задача 3.2 — распил раздутых скиллов (P6)
- **Все SKILL.md теперь ≤500 строк** (ADR-0001 hard cap). Топ после распила:
  configure-vpn-routing 500, sysadmin-init 493, setup-vpn-panel 490.
- **configure-vpn-routing 648→500:** Failed attempts + граничные случаи → `references/pitfalls.md`
  (132 стр.); в SKILL остался компактный блок «самое критичное по безопасности» + указатель.
  Свёрнут тройной дубль 7-правильной модели маршрутизации (канон в `<goals>`), сжаты
  менторские callout'ы (полный текст в pitfalls), inventory-шаблон, smoke, Откат. ВСЕ guard'ы
  и рефлексы (CONFIRM_MULTI_COUNTRY/CONFIRM_REALITY_ON_RU/exit 3/3.8.5/3.8.6) сохранены
  строкой+ссылкой; триггеры description не тронуты.
- **sysadmin-init 1000→493:** машинерия в scripts/ (ВСЕ протестированы против JSON-схем):
  `migrate-legacy.sh` (расщепление legacy), `assemble-configs.sh` (сборка обоих из env-ответов),
  `write-configs.sh` (запись+backup+FINAL CHECK). Bridge-указатель → общий `_lib/write-bridge.sh`
  (единый источник). Интервью свёрнуто в таблицу раундов (полные обёртки уже в wizard-flow.md).
  Failed Attempts + граничные → `references/edge-cases.md`.
- **finalize-vpn-routing/evals/triggers.md создан** (никогда не было — нарушение ADR-0001).
- Коммиты: afdd345 (evals: finalize+restore+dev), 5d8a9b3 (configure-vpn split),
  0dd9dee (вычистка «ученик»), fd8b9a8 (sysadmin-init split).

### [2026-06-13] Этап 3, задача 3.3 — валидатор знаний + pre-commit (P8)
- `scripts/validate-knowledge.py` — адаптация валидатора из проекта bb под СЛОИСТУЮ модель
  sysadmin (не Obsidian-граф): frontmatter (YAML + ключи + layer↔каталог + ttl↔канон
  14/60/365), битые ПУТЕВЫЕ ссылки (внутрь knowledge / в репо; голые имена не трогаем —
  `networks.md`/`sysadmin.md` живут вне knowledge, дали бы ложные срабатывания), сироты (мягко).
- Подключён в `.githooks/pre-commit`: переписан на FAIL-трекинг (раньше ранний `exit 0` при
  не-персона-коммите скрыл бы knowledge-проверку); персона и знания — две независимые ветки;
  python3 опционален (нет → WARN+пропуск, как check-jsonschema). Протестирован: pass exit 0,
  block exit 1, и отработал вживую на коммите 7cae426.
- Контроль свежести по датам НЕ дублирован — это `check-knowledge-freshness.sh` (персона §4.2).
- Валидатор сразу вскрыл 2 РЕАЛЬНЫХ дефекта (починены): subscription-mirroring.md был БЕЗ
  frontmatter (невидим для freshness — теперь виден); битый относительный ref в
  server-networks-defaults.md → полный путь. `/dev` обновлён (правило 5 + чеклист).
- Коммиты: 7cae426 (валидатор+pre-commit+fix), b9a41c7 (/dev доки).

### [2026-06-13] Этап 3, задача 3.4 — infra/ и чужие папки (P14/P15/P16)
- P14: `find_sysadmin_config` уважает env `SYSADMIN_PROJECT` (переключение проекта в сессии;
  контракт был в resolve_active_project, но всегда звался с ""). P15: мозг есть, но проект не
  разрешился → STOP, БЕЗ тихого fallback-перебора (мог увести на чужую инфру); fallback только
  при полном отсутствии мозга. P16: trust-zones.md — запись в чужую папку (мин. Yellow, чужой
  git не трогаю) + дисциплина коммита снимков (скиллы пишут, не коммитят); cold-start.md —
  SYSADMIN_PROJECT + STOP-на-карте. Протестировано (switch default/b/nonexistent, decoy не
  подхватывается). Коммит: 908f44e.

### [2026-06-13] Этап 3, задача 3.5 — масштаб и параллелизм (P21/P22)
- P21: честная память — «нативной памяти между сессиями нет, помню только файлы infra/»;
  устранена контрадикция потолков (≤200 vs ~1000) → хард-потолки (lessons-learned ≤1000,
  остальные ≤200) + обязательная `wc -l`-проверка и сжатие отдельным коммитом перед записью.
  §5 ядра + memory-ritual.md. P22: многосерверность честно задокументирована как частичная
  (cold-start.md: скиллы берут servers[0]; второй сервер — SSH_TARGET явно или отдельный
  проект); лок `inventory-scan` (атомарный mkdir, авто-снятие зависшего >30 мин, release Шаг 7).
  Протестировано (acquire/block/stale-break/release). Бюджет ядра 39042/42000. Коммит: d77edec.

### [2026-06-13] Этап 3, задача 3.6 — require_jq + релиз 2.1.0 (P10)
- P10: `require_jq` в find-config.sh (вызывается в find_brain_config — чокпойнт всех путей
  чтения конфига) → понятный STOP с инструкцией вместо криптового «jq: command not found»
  (jq у 19 скиллов, проверял только sysadmin-init). Протестирован (есть/нет jq).
- Релиз 2.1.0: VERSION + CHANGELOG [2.1.0] + UPGRADE (строка 2.0.x→2.1.0) + README.
- Коммиты: 4c946be (require_jq), a42d2c8 (release 2.1.0).

### [2026-06-13] Этап 3 — ЗАКРЫТ проверщиком (PASS 7/7)
- Свежий general-purpose проверщик: 7 критериев приёмки, все PASS, подтверждены кодом +
  живыми прогонами. Сам перепроверил P14 (переключение проектов: default→a,
  SYSADMIN_PROJECT=b→b на изолированном fake-мозге) и P15 (SYSADMIN_PROJECT=zzz → STOP rc=1,
  CONFIG пуст, без подхвата decoy). Обе заявленные регрессии проверены: retention пишется
  JSON-объектом (схема требует object); references (wizard-flow/edge-cases/pitfalls) непустые.
  Приукрашивания нет — журнал соответствует коду. Динамический тест валидатора знаний:
  битый файл → exit 1, мусора не осталось. Замечание (не дефект): тесты find-config на этой
  машине запускать с изолированным $HOME, иначе locate_sysadmin_root подхватывает боевой
  bridge/мозг оператора.
- Этап 3 закрыт, перехожу к Этапу 4 «Продукт».

### [2026-06-13] Этап 2 — ЗАКРЫТ проверщиком (PASS 13/13, регрессий нет)
- Свежий general-purpose проверщик: 13 пунктов, все PASS, все 5 критичных (существующие
  юзеры / конституция C.1–C.11 цела / бюджет enforced / линтер / тесты). Регрессий НЕТ.
  Ключевое: поиск корня работает без файла персоны (переименовал → ROOT OK через
  .sysadmin-root + CLAUDE.md-признак, legacy-признак оставлен); pre-commit реально
  блокирует раздувание (56082 > 42000 → exit 1); eval-харнес валиден (даже «правим персону»
  → dev). Незаблокирующее замечание: счёт скиллов — 21 SKILL.md; «20 операционных + /dev» —
  вопрос определения «операционный» (18 чисто-инфра + meet + init = 20, + dev = 21), не
  дефект, числа реальны. Свериться при правке состава (этап 3/4).
- Этап 2 закрыт, перехожу к Этапу 3 «Полировка».

### [2026-06-13] Этап 2, задача 2.7 — бамп 2.0.0 + CHANGELOG
- VERSION 1.12.0 → 2.0.0 (мажор: изменился вход). CHANGELOG: раздел [2.0.0] с явным блоком
  Breaking changes (вход «открыл папку → ты сисадмин», sysadmin.md → заглушка) + Добавлено
  (/dev, предохранители, бюджет в КБ ADR-0016, авто-bridge, новые references, ADR-0015/0016)
  + Изменено (персона на диете 692→393 строки, двухскоростной cold start, отмена 250-лимита,
  линтер/pre-commit/cold-start/find-config на CLAUDE.md) + Совместимость (infra не тронут,
  legacy конфиг читается, старый bridge самокорректируется).
- Коммит: (этот). Дальше — проверка Этапа 2 свежим агентом.

### [2026-06-13] Этап 2, задача 2.6 — документация под вход 2.0
- README: блок «Установка» — новый вход «открыл папку sysadmin/ → пиши напрямую»
  (cd sysadmin && claude; @sysadmin/`/add-dir` для чужих папок); секция «Агент-персона» —
  ядро в CLAUDE.md (≈390 строк, 9 references, бюджет 42 КБ ADR-0016), agents/sysadmin.md =
  заглушка; дерево репо — CLAUDE.md как ★ядро, sysadmin.md заглушка, +dev/ в скиллах,
  decisions 14→16 (+0015/0016), убраны дубли CLAUDE.md/INSTALL/VERSION, VERSION→2.0.0;
  раздел «Углубление» — порядок чтения через CLAUDE.md + references.
- INSTALL: bridge-heredoc (Шаг 7) → 2.0 (читать $PATH/CLAUDE.md, tools опущен → наследует
  Skill); completion-сообщение — основной способ «открыл папку», @sysadmin для чужих папок.
- sysadmin-meet: persona-location → CLAUDE.md (01-why-agent, 02-vocabulary, 07-where-next);
  04-how-to-talk — врезка «вход 2.0: открыл папку → пиши без @; @sysadmin из чужих папок».
- UPGRADE: таблица переходов → цель 2.0.0; добавлен ряд 1.11/1.12→2.0 (вход изменился,
  перезапусти /sysadmin-init для bridge, бюджет в КБ, 20+dev скиллов).
- Замечание: bridge создаётся в ДВУХ местах (INSTALL Шаг 7 — оркестрированная установка;
  sysadmin-init Шаг 10.4 — ручной clone). Оба → CLAUDE.md. Консолидация в общий скрипт —
  бэклог 3.2 (slimming sysadmin-init).
- Проверки: висячих «полный мозг агента»/старых путей в публичных доках нет; линтер PASS.
- Коммит: (этот)

### [2026-06-13] Этап 2, задачи 2.4 + 2.5 — бюджет мозга + двухскоростной Cold Start
- 2.4 (бюджет, ADR-0016): лимит ядра переведён со «строк» на **байты** (всегда-загружаемый
  файл — теперь CLAUDE.md). Бюджет **≤ 42 000 Б** (текущий 38 190 = 90%, остаток 3810 Б).
  pre-commit считает `wc -c CLAUDE.md` и **блокирует** коммит при превышении (проверено:
  при budget 30000 блокирует, при 42000 пропускает; bash -n OK). `/dev` первой строкой
  показывает расход бюджета (блок <budget-first>). Правило «одно входит — одно выходит» +
  анти-храповик (поднять бюджет — только ADR). Лимит «250 строк на references» **отменён**
  (ADR-0016): 3 файла его уже превышали законно (vpn-reflexes 446, cold-start 314,
  trust-zones 284), грузятся по триггеру; мягкий ориентир >~400 строк → подумать о разбиении.
- 2.5 (двухскоростной Cold Start): уже реализовано в ядре CLAUDE.md §7.1 (лёгкий путь для
  простых вопросов — мозг+inventory/README; полный путь 0-6 для серьёзной работы; первая
  просьба про правку мозга → сразу /dev) и §7.4 (старт = одна строка статуса, обязательные
  секции отменены). Добавлена врезка «два темпа» в cold-start.md для консистентности. Линтер PASS.
- Коммит: (этот)

### [2026-06-13] Этап 2, задача 2.3 — вход + предохранители + выездной bridge
- Адаптировано под ADR-0015 (вход через CLAUDE.md, не через settings.json `agent`):
  • settings.json `agent` НЕ создаю — указывал бы на тонкую заглушку, персона всё равно
    из CLAUDE.md; вход обеспечивает загрузка CLAUDE.md при открытии папки.
  • `.gitignore`: добавлен `.claude/settings.local.json` (личные переопределения, не
    авто-игнорятся); попутно убраны висячие negation-правила удалённых в 1.1 шаблонов
    `sysadmin-config.example/schema.json` (1.1-хвост).
  • Skill-инструмент: главный агент (CLAUDE.md) имеет Skill по умолчанию; заглушка
    наследует (tools опущен) — баг «персона без Skill» закрыт в 2.1.
  • Два предохранителя: §0 CLAUDE.md (в роли сисадмина правка мозга → /dev) и role-блок
    /dev (в /dev не сисадмин, на серверы не выходим) — оба на месте.
  • Выездной указатель: вскрыт пре-existing gap — `/sysadmin-init` НЕ создавал
    `~/.claude/agents/sysadmin.md` (self-test только ПРОВЕРЯЛ наличие, find-config ЧИТАЛ).
    Добавлен Шаг 10.4 в sysadmin-init: пишет bridge с абсолютным путём $SYSADMIN_ROOT и
    указанием читать CLAUDE.md (2.0-aware). Bash-синтаксис проверен.
  • `find-config.sh _is_sysadmin_root`: добавлен 2.0-признак корня (CLAUDE.md + .claude/skills),
    основной маркер по-прежнему `.sysadmin-root`; legacy-признак (agents/sysadmin.md) оставлен.
- Проверки: settings.local.json игнорится ✅, sysadmin-config.json игнорится ✅, root
  detection работает ✅, линтер PASS ✅.
- Коммит: (этот)

### [2026-06-13] Этап 2, задачи 2.1+2.2 — персона → CLAUDE.md + скилл /dev
- Сделаны вместе (две половины одного раскола: персона и dev-правила разъезжаются из
  старого CLAUDE.md). ADR-0015 зафиксирован.
- 2.1: новый `CLAUDE.md` = персона сисадмина на диете (393 строки против суммы старых
  слоёв 465+227=692). Конституция C.1–C.11 сохранена, рефлексы §3.8 — все 11 в компактной
  форме (триггер+правило+указатель), детали в references. Созданы 2 новых reference:
  `character-anchors.md` (§3.1–3.7 полностью) и `sysadmin-style.md` (регистр речи §1+§3.9).
  `.claude/agents/sysadmin.md` → тонкая заглушка-указатель на CLAUDE.md (не удалён —
  discovery + @sysadmin из чужих папок; tools опущен → наследует Skill). cold-start §1
  переписан (персона в CLAUDE.md, не в infra/). Линтер `check-persona-sync.sh` переведён
  на CLAUDE.md (PERSONA=CLAUDE.md, fallback на sysadmin.md для legacy). pre-commit ловит
  правки CLAUDE.md. **Линтер PASS** (C1–C5).
- 2.2: создан скилл `/dev` (конструктор мозга) — туда переехали dev-правила из старого
  CLAUDE.md: синхрон двухслойной персоны + линтер, «изменил скиллы → обнови доки»,
  «документ без ссылки», ADR-дисциплина, бюджет ядра, формат коммитов. Первая строка —
  предохранитель «ты конструктор, не сисадмин; на серверы не выходим». Скилл-дирижёр §0
  CLAUDE.md: в роли сисадмина правка мозга → переход в /dev. allowed-tools: Read/Write/
  Edit/Bash/Grep/Glob. Скилл-директорий стало 21 (20 операционных + dev); /dev — НЕ
  advertised operational, README «20» остаётся корректным для операционных.
- Резолв противоречия 2.1/2.3 (ADR-0015): вход — через CLAUDE.md; settings.json `agent`
  как механизм входа НЕ нужен. 2.3 адаптирую: settings.json минимальный без обязательного
  `agent`; README/INSTALL/sysadmin-meet под новый вход — в 2.6.
- Коммит: (этот)

### [2026-06-13] Этап 1 — ЗАКРЫТ проверщиком (PASS 9/9)
- Свежий general-purpose проверщик перечитал файлы, прогнал линтер + 5 живых
  eval-вызовов, проверил маппинг config-name. Вердикт PASS по всем 9 пунктам,
  регрессий нет. Незаблокирующее замечание: README не содержит инлайн-числа
  скиллов (есть только заголовок «### 20 готовых скиллов») — не дефект, неверного
  числа нет. Этап 1 закрыт, перехожу к Этапу 2.

Формат записи:
```
### [дата] Этап N, задача N.M — краткое название
- Что сделано: ...
- Коммит: <hash> <сообщение>
- Проверка: PASS/FAIL (если проверщик)
```

---

## Найдено и исправлено по ходу

> Мелкие дыры вне основного плана, которые агент починил на месте. Для прозрачности.

- [2026-06-13, после проверки этапа 0] Два наблюдения проверщика, починены сразу:
  (1) check-knowledge-freshness.sh шумел WARN на новые `_live/*.example.md`
  (плейсхолдер-даты) — теперь шаблоны исключены из проверки свежести;
  (2) подсказка Keychain в rotate-db-password.sh клала секрет в argv локального
  процесса (`-w "$(cat ...)"`) — заменена на скрытый интерактивный ввод (`-w` без
  значения).
- [2026-06-13, задача 3.2] **Латентный баг сборки конфига:** интервью Шаг 6 описывало
  `backups.retention` как «text, default 7d-4w-6m», а схема `infra-config` требует ОБЪЕКТ
  `{daily,weekly,monthly}` (`additionalProperties:false`). Старый иллюстративный bash собрал
  бы невалидный draft (Шаг 9 упал бы). Вскрыт тестом нового `assemble-configs.sh` против
  схемы; исправлено: скрипт пишет retention объектом, в SKILL добавлено явное предупреждение.
- [2026-06-13, задача 3.2] **Недостающие `evals/triggers.md`** (ADR-0001 требует у каждого
  скилла): finalize-vpn-routing (никогда не имел), restore-from-backup (создан в 3.1),
  dev (создан в 2.2) — мои пропуски. Созданы для всех трёх; теперь 22/22 скилла соответствуют.
- [2026-06-13, задача 3.2] **Остатки курс-лексики «ученик»** в активном мозге
  (vpn-reflexes.md ×3 + configure-vpn-routing ×2) — нарушение ADR-0007 (универсальный язык).
  Заменены на «оператор». Линтер персоны PASS.
- [2026-06-13, задача 3.3] **2 дефекта knowledge** (вскрыты новым валидатором):
  (1) `_reference/subscription-mirroring.md` БЕЗ frontmatter → был невидим для
  `check-knowledge-freshness.sh` (TTL не отслеживался); добавлен frontmatter
  (layer/ttl/last_researched 2026-05-22 из on-device-провенанса, sources_checked: []).
  (2) `server-networks-defaults.md` §409 — относительный ref `references/typical-grabli.md`
  не резолвился из knowledge; заменён на полный путь (как в §240 того же файла).

---

## Новые находки на решение оператора

> Крупное, что всплыло по ходу и меняет план или требует выбора. Агент НЕ чинит сам —
> заносит сюда и продолжает основной план.

_Пока пусто._

---

## Backlog (этап 5 и прочее на будущее)

_Заполняется по ходу. Стартовый список — в `01-ЭТАПЫ.md`, этап 5._

- **Дедуп `get_agent_field` (найдено в 3.2).** Функция `get_agent_field` есть как общий
  helper в `_lib/find-config.sh` (стр. ~429), но `configure-vpn-routing/SKILL.md` (Шаг 0a)
  определяет её inline копией. Заменить inline-копию на использование общей — убрать дубль.
  Не сделано в 3.2 (вне задачи распила; низкий приоритет, поведение идентично).
- **Тюнинг description/триггеров под терсные фразы (из baseline 1.4).** Модель
  отдаёт `none` на коротких/непрямых фразах: «первый запуск»→sysadmin-init,
  «помоги разобраться с агентом»→sysadmin-meet, «накати обновление»→deploy-service,
  «бот не может в Claude API»→setup-server-proxy. Плюс пересечение VPN-скиллов
  (configure-vpn-routing ↔ generate-client-config на «добавь клиента»). Уточнить
  description этих скиллов на этапе 3/4 (полировка), прогнать eval повторно. Это
  смена поведения маршрутизации — обсудить с оператором.
- **Ревизия negative-фраз в evals/triggers.md (из baseline 1.4).** Часть negative
  выглядит мис-дизайном: «настрой Grafana + Prometheus» как negative для
  install-monitoring-stack (фраза описывает сам скилл), «бэкап uploads» для
  setup-backups. Отделить осознанные пробы границ (restore→3.1) от ошибочных
  negative — отдельная задача чистки eval-данных.
