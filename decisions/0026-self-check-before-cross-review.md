# ADR-0026. Самопроверка замером перед сверкой — фильтр, а не замена внешнего ревью

**Дата:** 2026-08-18
**Статус:** Принято
**Автор:** режим `/dev` по решению оператора

## Контекст

ADR-0024 сделал кросс-ревью умолчанием: план и результат уходят на сверку вторым инженером
без запроса оператора. Правило работает, но оставляет открытым вопрос о состоянии кандидата
**в момент отправки**. Сейчас на сверку уходит первая связная редакция, и круг ревью
расходуется в том числе на дефекты, которые я мог измерить сам: несуществующий флаг,
число, не сходящееся с выводом команды, откат, записанный правдоподобно, но не проверенный.

Оператор поставил вопрос прямо: раз внешняя проверка почти всегда что-то находит, разумно
проверять себя до того, как отдавать работу наружу.

Против буквального прочтения «проверяй себя перед сдачей» в репозитории уже есть два
измерения, и оба говорят об одном:

- ADR-0022, раздел «Последствия»: «Внешняя сверка (ревью другим агентом) остаётся
  полезной именно потому, что самопроверка этот класс ошибок пропускает»;
- `references/trust-zones.md`, ловушка распознавания: самовыдача полномочий «изнутри
  ощущается не как обход правила, а как разумная экономия времени оператора — поэтому она и
  проходит самопроверку».

Класс, о котором идёт речь, — неверная предпосылка: она порождает и работу, и проверку этой
работы, поэтому перечитывание её не снимает. Все четыре дефекта сессии 2026-08-01, ради
которых заведён ADR-0024, были именно такими, и ни один из них агент не увидел сам.

Одновременно та же сессия дала обратное измерение: два вывода из шести, выданных сверкой,
были опровергнуты собственным замером агента. Значит арбитром выступает не инстанция, а
измерение — и его можно применить раньше, чем на приёме вердикта.

Рассматривался альтернативный вариант — самопроверка **вместо** первого круга сверки для
мелких правок. Отвергнут: условие остановки в ADR-0024 держится на чистом внешнем круге,
и подмена его собственным вердиктом снимает несущую конструкцию правила.

## Решение

> **Поправка 2026-08-20.** Ниже — исходная редакция решения. После первого внешнего круга
> проверок стало **шесть** (добавлена «контракт и цель»), отрицательный тест требует не только
> названия, но и выполнения, протокол вынесен в `references/self-check.md`, а строка «⚠️ КОПИЯ»
> принадлежит шагу PROPOSE, а не ASSESS. Действующая норма — раздел «Уточнение 2026-08-20»
> в конце файла; расхождения читать в его пользу.

Перед отправкой брифа на сверку кандидат проходит **проход самопроверки из пяти проверок**,
каждая из которых даёт ответ извне — от команды, файла или записи: числа прослежены до вывода
команды; упомянутые флаги, пути, файлы и версии подтверждены на инструменте; команда отката
исполнима и копия читается; вывод сверен с `inventory/` и `incidents/`; назван замер, который
вывод **опроверг бы**.

Проход — **замер, а не перечитывание**: перечитывание собственного текста не находит ничего,
потому что тот же ход мысли, что породил ошибку, породит и её одобрение.

Триггеры прохода совпадают с триггерами сверки (план и результат) — отдельного списка случаев
нет намеренно, две границы разошлись бы при первом же спорном случае.

Проход **не сокращает число кругов сверки, не смягчает условие остановки и не является исходом
приёма вердикта**. Его единственный эффект — круг сверки не расходуется на измеримое мной.
Пропущенный проход называется вслух так же, как пропущенная сверка.

В жёлтой и красной зонах проход не создаёт новой процедуры, а запрещает заполнять словами уже
существующие поля: пункт 4 брифинга («ЧТО ПРОВЕРИЛ») принимается только с выводом команды,
пункт 5 — с проверенной командой отката, строка «⚠️ КОПИЯ» шага PROPOSE пишется после того,
как копия прочитана.

## Последствия

- ✅ Дорогой инструмент тратится на дорогую работу: круг сверки уходит на вопрос «не порочна ли
  идея», а не на несуществующий флаг.
- ✅ Принцип «проверяемое проверяю замером», который в ADR-0024 действовал только на приёме
  вердикта, теперь работает и на входе — до того, как ошибка попадёт в чужой контекст.
- ✅ Отрицательный тест («чем мой вывод убивается») становится обязательным пунктом, а не
  разрозненным уроком: это единственная из пяти проверок, способная снять кандидат целиком.
- ⚠️ Каждое предъявление становится дороже по времени. Плата осознанная и меньшая, чем круг
  сверки, потраченный на механику.
- ⚠️ Главный риск — вырождение в ритуал: «я себя проверил» как ощущение вместо замера. Против
  него работает только форма проверок (ответ приходит извне) и прямой запрет считать проход
  заменой сверки; полностью риск не снимается.
- ⚠️ `references/cross-review.md` вырос до 439 строк. Ориентир ADR-0016 (~400 строк) превышен
  осознанно. *(Отменено 2026-08-20: файл дорос до 491 строки и держал два разных протокола;
  проход вынесен в `references/self-check.md`, cross-review вернулся к 425 строкам.)*

## Связанное

- Расширяет ADR-0024 (кросс-ревью по умолчанию): добавляет шаг перед отправкой брифа, не меняя
  триггеров, условия остановки и порядка «сверка до доклада и до действия».
- Опирается на ADR-0022 (продолжение — не полномочие): оттуда взят измеренный предел
  самопроверки, ради которого написан запрет на подмену.
- Влияет на: `CLAUDE.md` §3.10, `.claude/agents/references/self-check.md` (протокол прохода),
  `.claude/agents/references/cross-review.md` (указатель и требование к брифу),
  `.claude/agents/references/trust-zones.md` (брифинг Yellow, шаг PROPOSE в Red).

## Уточнение 2026-08-20 — после первого внешнего круга

Правило, введённое этим ADR, само внешней сверки не проходило: круг 18.08 не состоялся из-за
отозванной сессии Codex. Круг 20.08 состоялся и вынес «исполнять с правками». Внесено:

1. **Проход был неограниченным шлюзом.** Требование «пройти проход до отправки брифа» не имело
   ни статусов строк, ни исхода «не смог проверить», ни запрета задерживать круг — то есть
   позволяло не заменять сверку, но и не начинать её. Добавлены четыре статуса
   (пройдено / провалено / неприменимо / не проверено), форма записи строки и прямая норма:
   незакрытая строка запрещает объявлять готовность, но уходит в бриф с причиной, а не в ожидание.
2. **Шестая проверка — контракт и цель.** Диверсант круга: все проверки выполнены на копии в
   рабочем дереве, тогда как речь шла об активной установке; вывод «защищено» ложен при формально
   зелёном проходе. Закрывается только фиксацией исходного запроса, фактической цели и замером
   на ней самой.
3. **Отрицательный тест требует выполнения, а не называния.** Прежняя формулировка просила
   назвать опровергающий замер — назвать можно и недоступный.
4. **Мутационная поправка сужена до исполнимой формы:** минимум один изолированный, валидный,
   неэквивалентный мутант на каждую заявленную защиту, на одноразовой копии, с записью «что
   испорчено / что обязано упасть / что произошло»; выживший мутант означает либо дыру в стороже,
   либо неверно заявленную область. Прежнее «упал на каждом варианте» не задавало конечного
   множества и потому игнорировалось бы целиком.
5. **Свежесть замера формализована:** опоры называются заранее списком «что меняется → команда →
   идентификатор», и перед показом **или действием** те же команды повторяются. Одного `HEAD`
   мало: он краснеет на постороннем коммите и молчит про незакоммиченную правку.
6. **Фактический дефект:** строка «⚠️ КОПИЯ» была отнесена к шагу ASSESS, тогда как в
   `trust-zones.md` она принадлежит шагу PROPOSE. Исправлено.
7. **Ядро называло четыре проверки из пяти** (без «противоречий») и потеряло при сжатии
   защитные квалификаторы («сырой замер **без отбора**», «статус хода без причин и оценок»).
   Восстановлено. Место освобождено выносом карты репозитория в `references/repo-map.md`:
   41 991 → 41 416 Б, запас вырос с 9 до 584 Б без подъёма потолка.
8. **Протокол вынесен в отдельный файл** `references/self-check.md`: в `cross-review.md` остался
   указатель. Тот файл вырос до 491 строки и держал два разных протокола; после выноса — 425.

Не принято и названо честно: утверждение «два вывода из шести опровергнуты» (ADR-0024)
независимо переизмерить нельзя — сырого транскрипта нет; исторические мутанты
`tests/test-write-bridge.sh` не сохранены, поэтому тот замер невоспроизводим. Оба места остаются
свидетельствами, а не доказательствами.

## Уточнение 2026-08-20 (2) — после второго и контрольного кругов

Круг 2 искал регрессии переписки, круг 3 был контрольным и чистым не оказался. Внесено:

1. **План и результат проверяются по-разному.** Прежняя редакция требовала для плана
   постусловие, снятое end-to-end, и запрещала «неприменимо» — то есть план не мог быть
   пройден честно ни при каком исходе. Теперь у плана проверяются наблюдаемые **предпосылки**,
   а постусловие и проба **проектируются** со статусом `запланировано`.
2. **Переход «провалено → исправлено» назван.** Строка не стирается: кандидат правится, строка
   перевыполняется на новой ревизии, старая помечается снятой. Иначе выбор был между стиранием
   истории и вечным красным.
3. **Единица журнала уточнена:** тезис даёт столько строк, сколько проверок к нему применимо;
   в форму добавлено поле «какая из шести проверок».
4. **Секреты не попадают в журнал.** Журнал уходит ревьюеру целиком, а граница «секреты и
   приватная инфра вне доступа сверки» старше этого правила: вместо сырого факта —
   замаскированное наблюдение или ссылка на источник.
5. **«Кандидат прошёл» названо необходимым, но не достаточным условием готовности.** Готовой
   работу делает условие остановки внешней сверки, а не собственный проход.
6. **Покрытие требований проверяется в обе стороны** (диверсант круга 3): мало, чтобы каждый
   вывод отвечал запросу — каждое требование оператора обязано получить свой тезис. Требование
   без тезиса считается проваленным.
7. **Сторожа доведены и проверены мутацией:** C5 стал двусторонним (лишний шаг в ядре выживал),
   C4 смотрит окно с обеих сторон имени слоя и требует, чтобы любой соседний срок принадлежал
   канону (верное число рядом маскировало неверное), pre-commit прогоняет линтер **по
   staged-снимку** через `git checkout-index`, а не по рабочему дереву, фильтр расширен на
   удаления и смену типа файла, добавлен `pre-merge-commit`.
8. **`CLAUDE.md text eol=lf` внесён в `.gitattributes`:** канонический размер блоба, на котором
   держится бюджет ядра, опирался на локальный `core.autocrlf`, то есть на настройку машины.
9. **Канон ADR-0002 уточнён:** атрибуция цитаты не считается переходом вглубь, навигация
   считается. Без этого правило запрещало называть источник цитаты.

**Осознанно не принято.** Ревьюер предлагал версионированный журнал с `REQ-ID`, `@revision` и
`superseded-by`, то есть формальную двунаправленную матрицу требований. Взяты свойства, но не
формализм: покрытие в обе стороны и снятие строки ревизией записаны словами. Причина — цена:
это мозг агента одного оператора, а не процесс сертификации; матрица идентификаторов не
исполнялась бы в живом диалоге и выродилась бы в фикцию, против которой само правило и написано.
Решение пересмотреть, если появится повторяющийся класс потерь, который ловится только матрицей.

**Осталось открытым:** `--amend` только сообщения не запускает проверок (STAGED пуст — менять
нечего); мутационные доказательства хранятся в журнале сессии и сообщениях коммитов, а не
воспроизводимым набором в репозитории.

## Уточнение 2026-08-20 (3) — четвёртый круг, потолок

Круг 4 — контрольный и последний по правилу «потолок четыре круга». Вердикт: «исполнять с
правками», список блокирующего конечный. Закрыто:

1. **Статусная модель противоречила себе:** статусов объявлено четыре, а ниже вводился пятый
   (`запланировано`), и таблица требовала для плана выполненного постусловия. Теперь статусов
   пять явно, а критерии плана и результата стоят прямо в таблице проверок.
2. **Журнал свёрнут до одной строки на тезис** со статусами в ячейках: размножение строк по
   проверкам превращало его в матрицу, которую в живом диалоге не заполняют. Для мелкой работы
   оставлены три вопроса вместо таблицы; полная форма — для безопасности, необратимого, боевых
   данных, правил и разбора причин.
3. **Опора покрытия — дословный запрос оператора** первой строкой журнала. Формальные `REQ-ID`
   по-прежнему не заводятся, но проверять полноту относительно собственного пересказа нельзя:
   пересказ отфильтрован тем самым пониманием, которое проверяется. Этот довод ревьюера принят
   против моего прежнего отказа.
4. **C4 привязывает срок к слою**, а не ищет число рядом: мутант `_live/ — 60 дн (было 14 дн)`
   проходил, потому что верное число присутствовало, а оба были каноническими. Остаточная дыра
   названа честно: инвертированная формулировка `N дн для слоя` с каноническим, но чужим числом
   при наличии верного рядом не ловится — проверка обратного порядка даёт ложные срабатывания
   на перечислении слоёв и потому не введена.
5. **Сторож охраняет себя.** Правка линтера, хуков и `.gitattributes` теперь включает проверку,
   но одного этого мало: подменённый на `exit 0` линтер проходил, потому что его же и
   спрашивали. Поэтому pre-commit сначала прогоняет **приманку** — ядро со ссылкой в пустоту — и
   требует, чтобы линтер на ней покраснел. Не покраснел — сторож сломан или подменён, и его
   зелёный на настоящем ядре не значит ничего.
6. **Режим файла утверждается явно:** `references/*.md` в индексе обязан быть обычным файлом —
   symlink и gitlink проходили проверки существования.

**Предел процедуры записан отдельно** (диверсант круга 4): при византийском источнике —
скомпрометированном хосте, согласованно подделывающем вывод команд, конфиги и health-эндпоинт, —
зелёными оказываются и проход, и оба внешних круга, потому что независимость обеспечена по
моделям, а канал наблюдения у всех один. Для особо рискованного свидетельство берётся вторым
каналом; недоступен второй канал — ограничение произносится вслух.

**Правки этого круга внешней сверки не проходили:** потолок в четыре круга исчерпан, пятый —
решение оператора.
